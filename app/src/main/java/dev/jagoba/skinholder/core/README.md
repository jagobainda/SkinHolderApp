# core/

Clases base y contratos compartidos de la aplicación.

- `BaseViewModel`: ViewModel genérico con `StateFlow` para gestión de estado UI.
- Aquí se añadirán interfaces base, clases abstractas y utilidades de arquitectura comunes a todas las capas.
